# 🎨 UI/UX 디자인 가이드라인 (2026 트렌드 반영)

> **리퀴드 글래스 × 벤토 그리드 2.0 × 촉각적 맥시멀리즘**

---

## 1. 시각 언어: 리퀴드 글래스 (Liquid Glass)

### 1.1 핵심 디자인 원칙

애플의 2026년 디자인 언어를 차용한 '리퀴드 글래스' 스타일 적용

```css
/* 리퀴드 글래스 기본 스타일 */
.liquid-glass-card {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 24px;
  box-shadow: 
    0 8px 32px rgba(0, 0, 0, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.4);
}

/* 시간대별 배경 그라데이션 */
.bg-morning {
  background: linear-gradient(135deg, #FFE5B4 0%, #FFECD2 50%, #FCB69F 100%);
}

.bg-afternoon {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
}

.bg-evening {
  background: linear-gradient(135deg, #2c3e50 0%, #4ca1af 50%, #2c3e50 100%);
}

.bg-night {
  background: linear-gradient(135deg, #0c0c0c 0%, #1a1a2e 50%, #16213e 100%);
}
```

### 1.2 실시간 배경 변화 시스템

| 조건 | 배경 효과 | 카드 이펙트 |
|------|----------|-------------|
| 맑은 아침 | 따뜻한 골든 그라데이션 | 부드러운 그림자 |
| 흐린 날 | 소프트 그레이 톤 | 은은한 빛 굴절 |
| 비오는 날 | 블루 그레이 + 물방울 효과 | 유리에 빗방울 패턴 |
| 밤 | 다크 모드 + 별빛 파티클 | 네온 엣지 글로우 |

### 1.3 중요도 기반 시선 유도

```javascript
// 중요 카드 하이라이트 로직
function applyImportanceGlow(taskCard, importanceLevel) {
  const glowIntensity = {
    low: '0 0 10px rgba(255, 255, 255, 0.1)',
    medium: '0 0 20px rgba(103, 126, 234, 0.3)',
    high: '0 0 30px rgba(255, 107, 107, 0.5), 0 0 60px rgba(255, 107, 107, 0.2)',
    urgent: '0 0 40px rgba(255, 71, 87, 0.6), inset 0 0 20px rgba(255, 71, 87, 0.1)'
  };
  
  taskCard.style.boxShadow = glowIntensity[importanceLevel];
  
  // 긴급한 경우 부드러운 펄스 애니메이션
  if (importanceLevel === 'urgent') {
    taskCard.classList.add('pulse-glow');
  }
}
```

---

## 2. 레이아웃: 벤토 그리드 2.0

### 2.1 반응형 모듈 시스템

시간대에 따라 모듈 크기가 동적으로 변화

```
┌─────────────────────────────────────────────────────┐
│                    오전 레이아웃                      │
├─────────────────────────────────────────────────────┤
│  ┌───────────────────────┐ ┌───────────┐           │
│  │                       │ │           │           │
│  │   📋 오늘의 할일       │ │ 🐣 오롤이  │           │
│  │   (HERO - 2x2)        │ │  (1x1)    │           │
│  │                       │ │           │           │
│  │   ☐ 영어 공부 30분    │ └───────────┘           │
│  │   ☐ 운동하기          │ ┌───────────┐           │
│  │   ☐ 독서 20분         │ │ 📊 진행률  │           │
│  │                       │ │  (1x1)    │           │
│  └───────────────────────┘ └───────────┘           │
│                                                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────────────────┐   │
│  │⏱️ 타이머│ │📅 캘린더 │ │ 💌 친구 소식 (2x1) │   │
│  │ (1x1)   │ │ (1x1)   │ │                     │   │
│  └─────────┘ └─────────┘ └─────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                    오후 레이아웃                      │
├─────────────────────────────────────────────────────┤
│  ┌───────────┐ ┌───────────────────────────────┐   │
│  │ 📋 할일   │ │                               │   │
│  │  (1x1)   │ │     📊 진행률 추적기           │   │
│  └───────────┘ │        (HERO - 2x2)           │   │
│  ┌───────────┐ │                               │   │
│  │ 🐣 오롤이  │ │   ████████████░░░  78%       │   │
│  │  (1x1)   │ │                               │   │
│  └───────────┘ └───────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 2.2 모듈 가중치 계산

```javascript
// 시간대별 모듈 가중치
const TIME_WEIGHTS = {
  morning: {    // 5-9시
    todoList: 1.0,
    progress: 0.3,
    pet: 0.5,
    social: 0.2,
    timer: 0.4
  },
  workHours: {  // 9-18시
    todoList: 0.6,
    progress: 0.8,
    pet: 0.3,
    social: 0.2,
    timer: 1.0
  },
  evening: {    // 18-22시
    todoList: 0.4,
    progress: 0.6,
    pet: 0.7,
    social: 1.0,
    timer: 0.2
  },
  night: {      // 22-5시
    todoList: 0.2,
    progress: 0.3,
    pet: 0.8,
    social: 0.4,
    timer: 0.1
  }
};

function calculateHeroModule(currentTime, userBehavior) {
  const timeSlot = getTimeSlot(currentTime);
  const weights = TIME_WEIGHTS[timeSlot];
  
  // 사용자 행동 데이터로 가중치 조정
  Object.keys(weights).forEach(module => {
    weights[module] *= userBehavior.preferenceScore[module] || 1;
  });
  
  return Object.entries(weights)
    .sort(([,a], [,b]) => b - a)[0][0]; // 최고 가중치 모듈 반환
}
```

---

## 3. 마이크로 인터랙션: 촉각적 맥시멀리즘

### 3.1 할일 완료 인터랙션

**효과 1: 리퀴드 체크 (Liquid Check)**
```
체크박스 클릭 시:
1. 체크 아이콘이 물방울처럼 퍼지며 나타남
2. 체크박스 경계가 액체처럼 흐르며 사라짐
3. 카드가 부드럽게 아래로 밀려남
4. 햅틱: 부드러운 '톡' 피드백
```

**효과 2: 컨페티 버스트 (Confetti Burst)**
```
오늘 목표 100% 달성 시:
1. 화면 중앙에서 작은 폭발 효과
2. 다양한 색상의 컨페티가 중력 영향으로 떨어짐
3. 오롤이 캐릭터가 점프 애니메이션
4. 햅틱: 연속 진동 패턴
```

**효과 3: 웨이브 리플 (Wave Ripple)**
```
친구 스티커 수신 시:
1. 알림이 도착하는 방향에서 물결 파동
2. 스티커가 부드럽게 '착지'하는 애니메이션
3. 잔잔한 반향 효과
4. 햅틱: 심장 박동 같은 리듬
```

### 3.2 CSS 애니메이션 코드

```css
/* 리퀴드 체크 애니메이션 */
@keyframes liquidCheck {
  0% { transform: scale(0); opacity: 0; }
  50% { transform: scale(1.3); opacity: 1; }
  70% { transform: scale(0.9); }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes cardSlideDown {
  0% { transform: translateY(0); opacity: 1; }
  100% { transform: translateY(20px); opacity: 0; height: 0; margin: 0; padding: 0; }
}

/* 컨페티 파티클 */
@keyframes confettiFall {
  0% { transform: translateY(-100vh) rotate(0deg); opacity: 1; }
  100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
}

/* 웨이브 리플 */
@keyframes waveRipple {
  0% { transform: scale(0); opacity: 0.8; }
  100% { transform: scale(4); opacity: 0; }
}

.check-animation {
  animation: liquidCheck 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.completed-card {
  animation: cardSlideDown 0.5s ease-out forwards;
  animation-delay: 0.3s;
}
```

---

## 4. 컬러 팔레트

### 4.1 메인 컬러 시스템

```css
:root {
  /* Primary - 따뜻한 코랄 */
  --primary-50: #FFF5F3;
  --primary-100: #FFE4DF;
  --primary-300: #FFB3A7;
  --primary-500: #FF6B6B;
  --primary-700: #E84545;
  
  /* Secondary - 차분한 퍼플 */
  --secondary-50: #F5F3FF;
  --secondary-100: #EDE9FE;
  --secondary-300: #C4B5FD;
  --secondary-500: #8B5CF6;
  --secondary-700: #6D28D9;
  
  /* Accent - 민트 */
  --accent-50: #F0FDFA;
  --accent-300: #5EEAD4;
  --accent-500: #14B8A6;
  
  /* Neutral */
  --gray-50: #FAFAFA;
  --gray-100: #F4F4F5;
  --gray-200: #E4E4E7;
  --gray-500: #71717A;
  --gray-800: #27272A;
  --gray-900: #18181B;
  
  /* Semantic */
  --success: #22C55E;
  --warning: #FBBF24;
  --error: #EF4444;
  --info: #3B82F6;
}
```

---

## 5. 타이포그래피

```css
/* Google Fonts 임포트 */
@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap');

:root {
  --font-display: 'Outfit', 'Noto Sans KR', sans-serif;
  --font-body: 'Noto Sans KR', sans-serif;
  
  /* 타입 스케일 */
  --text-3xl: 2rem;      /* 32px - 대제목 */
  --text-2xl: 1.5rem;    /* 24px - 섹션 제목 */
  --text-xl: 1.25rem;    /* 20px - 카드 제목 */
  --text-lg: 1.125rem;   /* 18px - 강조 본문 */
  --text-base: 1rem;     /* 16px - 본문 */
  --text-sm: 0.875rem;   /* 14px - 보조 텍스트 */
  --text-xs: 0.75rem;    /* 12px - 라벨 */
}

h1, h2, h3 {
  font-family: var(--font-display);
  font-weight: 600;
  letter-spacing: -0.02em;
}

body {
  font-family: var(--font-body);
  font-weight: 400;
  line-height: 1.6;
}
```

---

## 6. 아이콘 & 일러스트레이션 가이드

### 6.1 아이콘 스타일
- **라인 두께**: 2px (일관성 유지)
- **모서리**: 라운드 처리
- **크기**: 24x24px (기본), 32x32px (강조)
- **컬러**: 단색, 상황에 따라 그라데이션 적용

### 6.2 캐릭터 (오롤이)
- **스타일**: 미니멀하지만 표정이 풍부한 2.5D
- **표현**: 감정에 따른 다양한 포즈 (기쁨, 응원, 졸림 등)
- **애니메이션**: Lottie 또는 Rive로 구현

---

다음: [03. 스케줄링 알고리즘 설계](./03_스케줄링_알고리즘_설계.md)
